import { ValidSelectedDirective } from './valid-selected.directive';

describe('ValidSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
